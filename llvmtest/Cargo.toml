[package]
name = "llvmtest"
version = "0.1.0"
edition = "2024"

[dependencies]
inkwell = { version = "0.9.0", default-features = false, features = [
  "llvm22-1-force-static",
  "target-x86",
] }
sajit = { version = "^0", path = "..", features = ["llvm"] }

[workspace]
