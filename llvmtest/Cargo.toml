[package]
name = "llvmtest"
version = "0.1.0"
edition = "2024"

[dependencies]
inkwell = { version = "0.9.0", default-features = false, features = [
  "llvm21-1-force-static",
  "target-all",
] }
sajit = { version = "0.0.1", path = "..", features = ["llvm"] }

[workspace]
